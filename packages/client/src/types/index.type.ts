type TTheme = "dark" | "light" | "system";

enum InteractionMode {
  NONE = "none",
  DELETE = "delete",
  REPLY = "reply",
  REACT = "react",
  PIN = "pin",
  drag = "drag",
}

export { type TTheme };
export { InteractionMode };
